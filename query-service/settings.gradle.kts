// Independent build, like the other services. query-service is the read side's HTTP face:
// it reads the projections that projection-service maintains and writes nothing.
rootProject.name = "query-service"
